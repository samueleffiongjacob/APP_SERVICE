export type ServiceRequest = {
  id: string;
  name: string;
  email: string;
  phone: string;
  service: string;
  message: string;
  createdAt: string;
};

export type AppUser = {
  id: string;
  name: string;
  email: string;
  phone: string;
  createdAt: string;
};
